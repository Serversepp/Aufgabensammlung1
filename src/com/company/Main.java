package com.company;


public class Main {

    public static void main(String[] args) {
        //if (Integer.parseInt(args[0]) < 1) return;
        //for (int i = 0; i < Integer.parseInt(args[0]); i++) {
           // for (int j = 0; j <= i; j++) {
           //     System.out.print("*");
          //  }
          //  System.out.printf("\n");
       // }
        //INOUTApp App1 = new INOUTApp();
        //App1.Ausgabe();

        //Beton Misch1 = new Beton();
        // System.out.println("Benötigte Anzahl LKWs: " + Misch1.berechnen());

        //TriangleApp Dreieck = new TriangleApp();
        //System.out. println(Dreieck.berechnen());

        //InterestApp Zinsen = new InterestApp();
        //Zinsen.Ausgabe();

        //RectangleApp Rechteck = new RectangleApp();
        //Rechteck.Ausgabe();

        //System.out.println(AgeApp.judge());

        //CalculatorApp.Calc();
        //Encoder Enc = new Encoder();
        //int[] arr = new int[5];
        //Enc.input(arr);
        //Enc.print(arr);
        //Enc.print(Enc.decode(Enc.encode(arr)));
        //Enc.print(Enc.encode(arr));





        /*
        Encoder worker = new Encoder();
        // Ein Array mit 15 Werten anlegen und weitere Referenzen
        int[] values = new int[15];
        int[] reversedArray, encodedArray, decodedArray;
        // Werte in Eingabe-Array einlesen und ausgeben
        worker.input(values);
        System.out.println(" ----------- Eingabewerte: ----");
        worker.print(values);

        // Eingabe-Array drehen und ausgeben
        reversedArray = worker.reverse(values);
        System.out.println(" ----------- Nach Drehung: ----");
        worker.print(reversedArray);
        // Eingabe-Array komprimieren und ausgeben
        encodedArray = worker.encode(values);
        System.out.println (" ---------- Komprimiert: ----");
        worker.print(encodedArray);
        // Komprimiertes Array dekomprieren und ausgeben
        System.out.println (" ---------- Dekomprimiert: ----");
        decodedArray = worker.decode(encodedArray);
        worker.print(decodedArray);
        // Ergebnis vergleichen
        if (worker.compare(values, decodedArray))
            System.out.println("OK - Dekodierung erfolgreicht!");
        else
            System.out.println("Nicht OK - Fehler bei der Dekodierung!");*/


        //LottoApp.main();

        //MatrixApp.main();

        CaesarApp.main(6,false,"iutyzgtzot ngz tkt qrkotkt vosskr");
    }
}
